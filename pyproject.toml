[project]
name = "ops_ai_cortex"
version = "0.1.0"
description = "AI-driven DevOps intelligence platform"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
  "fastapi",
  "langchain",
  "redis",
  "psycopg2-binary",
  "anthropic",
  "openai",
  "pydantic",
]

[tool.uv]
dev-dependencies = [
  "pytest",
  "black",
  "ruff",
  "mypy",
  "httpx",
  "pytest-asyncio"
]
